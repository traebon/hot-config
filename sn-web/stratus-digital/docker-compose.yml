services:
  stratus-digital:
    image: nginx:alpine
    container_name: stratus-digital
    restart: always
    volumes:
      - ./html:/usr/share/nginx/html:ro
    ports:
      - "10.10.30.102:8001:80"
