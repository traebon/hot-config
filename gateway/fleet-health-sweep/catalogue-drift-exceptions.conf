# Accepted live-vs-approved binding mismatches for the catalogue-binding-drift check
# (docs/HoT_Automation_Self_Healing_Scope.md Section 8). One entry per line:
#   slug:container_name:live_host_ip:live_host_port
# Only add an entry here after Mr. Byrne has explicitly decided to formalize a specific
# drifted binding rather than revert it -- this file is what keeps the check from
# re-flagging that one, specific, already-decided case every night. It is NOT a general
# silence mechanism; anything not listed here still fails and escalates.

# nextcloud: rebound 2026-08-11 (unattributed, outside governance) from the originally
# approved 127.0.0.1:28142 to 10.10.2.2:28142 (wg3 tunnel IP). PN's own health check
# (health_endpoint, wired 2026-08-19) depends on this binding. Mr. Byrne chose to
# formalize rather than revert, 2026-08-20 -- see
# pn_nextcloud_binding_and_personal_cleanup_2026_08_20 memory and services-hotpn.md.
nextcloud:nextcloud:10.10.2.2:28142

# notesnook: all 4 rebound 2026-08-11T13:29 UTC (same unattributed pattern as nextcloud,
# ~10h later same day), from either unpublished (s3/identity/sse had no `ports:` at all
# in the approved compose) or a different host port (sync: approved 127.0.0.1:23683,
# live 10.10.2.2:5264) to their current 10.10.2.2 bindings.
#
# Correction, 2026-08-20: originally assessed s3/sync/sse as having "no known consumer"
# (checked only PN's services.health_endpoint, which references identity alone) and
# reverted those three per that read -- WRONG. Caddy's live Caddyfile has real public
# site blocks (notes-sync/notes-sse/notes-s3.house-of-trae.com) reverse-proxying
# directly to these exact 10.10.2.2 bindings, wired the same day (2026-08-11) as the
# container recreate -- i.e. this was real, deliberate production wiring work, just
# never reflected back into the governance record or documented anywhere. The revert
# caused a real ~2-5 minute outage on those 3 public endpoints (502s), caught via a
# direct check against the live URLs, and immediately fixed by recreating all 3 back to
# their pre-revert state. All 4 formalized together after that correction -- see
# pn_nextcloud_binding_and_personal_cleanup_2026_08_20 memory for the full incident
# trace and services-hotpn.md.
notesnook:notesnook-s3:10.10.2.2:9000
notesnook:notesnook-identity:10.10.2.2:8264
notesnook:notesnook-sync:10.10.2.2:5264
notesnook:notesnook-sse:10.10.2.2:7264
